import * as styles from './style.css';

function App() {
  return (
    <div className={styles.themeClass}>
      <div className={`App ${styles.appStyle} object`}>쉼쉼 초기셋팅 use Vite Bundler</div>
    </div>
  );
}

export default App;
